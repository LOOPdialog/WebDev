import React from 'react';
import clasess from './index.module.scss';

const DealersPage = (): React.ReactElement => {
  return (
    <div className={clasess.mainWrapperDealersPage}>
      DealersPage
    </div>
  );
};

export default DealersPage;
